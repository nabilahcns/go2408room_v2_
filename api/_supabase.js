import crypto from 'crypto';
export async function sb(path,options={}){const base=process.env.SUPABASE_URL,key=process.env.SUPABASE_SECRET_KEY;if(!base||!key)throw Error('Supabase environment variable belum lengkap.');const r=await fetch(`${base}/rest/v1/${path}`,{...options,headers:{apikey:key,Authorization:`Bearer ${key}`,'Content-Type':'application/json',...(options.headers||{})}});const t=await r.text();let d=null;try{d=t?JSON.parse(t):null}catch{d=t}if(!r.ok)throw Error(d?.message||d?.hint||d?.error||'Supabase request gagal.');return d}
export function isAdmin(req){try{const m=(req.headers.cookie||'').match(/admin_session=([^;]+)/);const [e,s]=String(m?.[1]||'').split('.');if(!e||!s)return false;const x=crypto.createHmac('sha256',process.env.AUTH_SECRET).update(e).digest('base64url');if(x!==s)return false;const d=JSON.parse(Buffer.from(e,'base64url').toString());return d.exp>Date.now()&&d.username===process.env.ADMIN_USERNAME}catch{return false}}
